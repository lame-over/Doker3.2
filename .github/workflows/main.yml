name: Lint
on: [push]
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
  
    - name: Validate package.json     
    - name: Install dependencies
      run: npm install
